targets:
  $default:
    builders:
      riverpod_generator:
        options:
          riverpod_version: 2
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: snake
      retrofit_generator:
        options:
          auto_headers: false
